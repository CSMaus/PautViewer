<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:sys="clr-namespace:System;assembly=mscorlib">
    <FontFamily x:Key="StyleFontFamily">Palatino</FontFamily>
    <sys:Double x:Key="StandardFontSize">12</sys:Double>


    <Style x:Key="SlimTextBox"
           TargetType="TextBox"
           BasedOn="{StaticResource MahApps.Styles.TextBox}">
        <Setter Property="FontSize" Value="11" />
        <Setter Property="Padding" Value="2,0,2,0" />
        <Setter Property="MinHeight" Value="18" />
        <Setter Property="Height" Value="18" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
    </Style>

    <!-- Slim Button based on MahApps -->
    <Style x:Key="SlimButton"
           TargetType="Button"
           BasedOn="{StaticResource MahApps.Styles.Button}">
        <Setter Property="FontSize" Value="11" />
        <Setter Property="Padding" Value="4,0,4,0" />
        <Setter Property="MinHeight" Value="18" />
        <Setter Property="Height" Value="18" />
    </Style>
    
    <!-- GridSplitter -->
    <Style x:Key="GradientGridSplitterStyleH" TargetType="{x:Type GridSplitter}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type GridSplitter}">
                    <Border Background="{TemplateBinding Background}">
                        <Rectangle>
                            <Rectangle.Fill>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                    <GradientStop Color="{DynamicResource MahApps.Colors.Gray8}" />
                                </LinearGradientBrush>
                            </Rectangle.Fill>
                        </Rectangle>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="GradientGridSplitterStyleV" TargetType="{x:Type GridSplitter}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type GridSplitter}">
                    <Border Background="{TemplateBinding Background}">
                        <Rectangle>
                            <Rectangle.Fill>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                    <GradientStop Color="{DynamicResource MahApps.Colors.Gray8}" Offset="0.0" />
                                    <GradientStop Color="{DynamicResource MahApps.Colors.Gray8}" Offset="0.5" />
                                    <GradientStop Color="{DynamicResource MahApps.Colors.Gray8}" Offset="1.0" />
                                </LinearGradientBrush>
                            </Rectangle.Fill>
                        </Rectangle>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>



</ResourceDictionary>